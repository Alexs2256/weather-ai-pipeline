[project]
name = "weather-pipeline"
version = "0.1.0"
description = "Weather ETL pipeline with Airflow"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["plugins", "plugins.weather_pipeline"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"